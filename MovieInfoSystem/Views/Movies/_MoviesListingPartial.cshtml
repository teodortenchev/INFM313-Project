@model List<MovieListingServiceModel>

<div class="row">
    @foreach (var movie in Model)
    {
        <div class="p-lg-2">
            <div class="card" style="width: 16rem; padding:7px;">
                <img height="350" class="card-img-top" src="@movie.Image" alt="Card image cap">
                <div class="card-body">
                    <h6 class="card-title">@movie.Title</h6>
                    <p class="card-text">@movie.Summary.Substring(0, 50)...</p>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        <span class="font-weight-bold">Actors:</span>
                        <span>
                            @movie.Actors.Select(a => a.FirstName + " " + a.LastName).First()...
                        </span>
                    </li>
                    <li class="list-group-item">
                        <span class="font-weight-bold">Directors:</span>
                        <span>
                            @string.Join(", ", movie.Directors.Select(d => d.FirstName + " " + d.LastName))
                        </span>
                    </li>
                    <li class="list-group-item">
                        <span class="font-weight-bold">Countries:</span>
                        <span>
                            @string.Join(", ", movie.Countries.Select(c => c.Name))
                        </span>
                    </li>
                </ul>
                <div class="card-body">
                    <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.Id" class="card-link btn btn-primary text-center">Details</a>
                </div>
            </div>
        </div>
    }
</div>